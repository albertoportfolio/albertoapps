---
import { projects, site } from "../data/site";
---

<section id="projects" class="section">
  <div class="container">
    <div class="section-head reveal">
      <span class="section-kicker">Portfolio</span>
      <h2 class="section-title">Featured Projects</h2>
      <p class="section-sub">
        A selection of apps and websites I've developed. You can check out
        the code on my GitHub.
      </p>
    </div>

    <div class="projects-grid">
      {projects.map((p, i) => (
        <article class="card project-card reveal" style={`--rd: ${i * 0.08}s`}>
          <div class="project-top">
            <span class="project-folder" aria-hidden="true">📂</span>
            <div class="project-links">
              <a href={p.repo} target="_blank" rel="noopener noreferrer">
                Code ↗
              </a>
              {p.demo && (
                <a href={p.demo} target="_blank" rel="noopener noreferrer">
                  Demo ↗
                </a>
              )}
            </div>
          </div>
          <h3>{p.title}</h3>
          <p>{p.description}</p>
          <div class="tags">
            {p.tags.map((t) => (
              <span class="tag">{t}</span>
            ))}
          </div>
        </article>
      ))}
    </div>

    <div class="projects-cta reveal reveal-fade" style="--rd: 0.2s">
      <a
        href={site.github}
        target="_blank"
        rel="noopener noreferrer"
        class="btn btn-ghost"
      >
        See more on GitHub →
      </a>
    </div>
  </div>
</section>